V43.7.3 — PACOTE FINAL PARA SOBREPOSIÇÃO

BASE ESPERADA
V43.7.2 / estado atual da branch main no momento da criação.

COMO APLICAR
1. Extraia este ZIP diretamente na raiz do repositório.
2. Permita substituir:
   js/core/config.js
3. Os outros dois arquivos são novos:
   css/pages/v43-7-3.css
   js/pages/home/v43-7-3.js
4. Não é necessário executar Python, npm, build ou instalador.
5. Rode a CI normal do repositório antes do merge.

ARQUIVOS DO PACOTE
- js/core/config.js
- css/pages/v43-7-3.css
- js/pages/home/v43-7-3.js
- V43-7-3-HOTFIX.txt

ALTERAÇÕES VISÍVEIS
- Hero:
  "Apoiar" recebe o mesmo desenho de coração SVG usado na navbar.

- Card "Gostou das lives?":
  o rótulo renderizado passa a ser "Apoiar" e recebe o mesmo coração.

- Carrossel "Últimas lives":
  passa a aceitar click + arrasta horizontal com mouse/caneta.

- Carrossel "Agenda da semana":
  passa a aceitar click + arrasta horizontal com mouse/caneta.

COMPORTAMENTO PRESERVADO
- clique normal nos cards de Lives;
- touch nativo;
- trackpad/roda;
- teclado;
- botões anterior/próximo;
- scroll-snap quando não está arrastando;
- confirmação global de links externos;
- responsividade existente.

ISOLAMENTO
O objeto KAMYLI_CONFIG e KAMYLI_SITE_PATH permanecem exatamente iguais ao
estado atual. O trecho V43.7.3 anexado ao config.js serve apenas de bootstrap:
se #inicio não existe, ele encerra imediatamente e não carrega os arquivos
da V43.7.3.

Assim, /doacoes/ não recebe o módulo visual/comportamental da V43.7.3.

NÃO ALTERA
- Worker;
- OAuth;
- KV;
- CORS;
- ranking;
- navbar;
- footer;
- loader;
- lives.js;
- content.js;
- home.js;
- home.css;
- SEO;
- assets;
- demais textos/cards.

OBSERVAÇÃO SOBRE O TEXTO DO CTA
O rótulo "Apoiar" do card "Gostou das lives?" é aplicado na renderização
pela V43.7.3, inclusive depois que o JSON editorial da Home é carregado.
O JSON original não é modificado por este hotfix.
