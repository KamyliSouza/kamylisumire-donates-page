diff --git a/.github/scripts/sync-jogos.mjs b/.github/scripts/sync-jogos.mjs
index 3b81178..fd728f0 100644
--- a/.github/scripts/sync-jogos.mjs
+++ b/.github/scripts/sync-jogos.mjs
@@ -107,7 +107,8 @@ for (const card of cards) {
   } catch (error) {
     console.warn(`SteamGridDB: ${card.name}: ${error.message}`);
   }
-  games.push({ id: card.id, name: card.name, listId: list.id, listName: list.name, pos: card.pos, artwork });
+  const steamUrl = artwork?.steamAppId ? `https://store.steampowered.com/app/${artwork.steamAppId}/` : null;
+  games.push({ id: card.id, name: card.name, listId: list.id, listName: list.name, pos: card.pos, artwork, steamUrl });
 }
 
 let previous = null;
diff --git a/.github/scripts/validate-content.py b/.github/scripts/validate-content.py
index f512d2f..e2dd458 100644
--- a/.github/scripts/validate-content.py
+++ b/.github/scripts/validate-content.py
@@ -836,6 +836,14 @@ def validate_jogos() -> None:
                 error(f"data/content/jogos.json: artwork.url deve ser HTTPS em {game['id']}.")
             elif artwork.get("provider") == "steam-original" and not artwork["url"].startswith("https://cdn.cloudflare.steamstatic.com/steam/apps/"):
                 error(f"data/content/jogos.json: asset Steam original deve usar a Steam CDN em {game['id']}.")
+        steam_url = game.get("steamUrl")
+        if steam_url is not None:
+            if not isinstance(steam_url, str) or not re.fullmatch(r"https://store\.steampowered\.com/app/[0-9]+/", steam_url):
+                error(f"data/content/jogos.json: steamUrl inválida em {game['id']}.")
+            if not isinstance(artwork, dict) or artwork.get("provider") != "steam-original" or not isinstance(artwork.get("steamAppId"), int):
+                error(f"data/content/jogos.json: steamUrl exige steamAppId confirmado em {game['id']}.")
+            elif steam_url != f"https://store.steampowered.com/app/{artwork['steamAppId']}/":
+                error(f"data/content/jogos.json: steamUrl não corresponde ao steamAppId em {game['id']}.")
 
     navbar = load_json("data/content/navbar.json")
     jogos_link = navbar.get("links", {}).get("jogos", {}) if isinstance(navbar, dict) else {}
diff --git a/CHANGELOG.md b/CHANGELOG.md
index a1cdf24..f4d174d 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,13 @@
 # Changelog
 
+## V48.3.16 — Hotfix: cards de Jogos e link Steam
+
+- aproxima os cards de `/jogos/` da linguagem visual da Galeria, com borda/sombra, elevação discreta e zoom suave da capa;
+- move título e categoria/lista para uma sobreposição no rodapé da imagem com gradiente/sombra, preservando legibilidade;
+- remove a borda interna do placeholder, mantendo apenas o fallback visual integrado ao site;
+- publica `steamUrl` somente quando a sincronização já confirmou um `steamAppId`, e exibe **Ver na Steam** apenas nesses casos;
+- mantém Trello, Original Steam Assets, Worker e demais páginas sem mudanças de contrato ou comportamento.
+
 ## V48.3.15 — Hotfix: assets originais e placeholder de Jogos
 
 - deixa de publicar automaticamente grids comunitários do SteamGridDB: a busca passa a servir somente para confirmar uma correspondência Steam exata e inequívoca;
diff --git a/css/pages/jogos.css b/css/pages/jogos.css
index edc0758..7957c63 100644
--- a/css/pages/jogos.css
+++ b/css/pages/jogos.css
@@ -1,4 +1,4 @@
-/* JOGOS — V48.3.15. Mantém a página no sistema visual existente. */
+/* JOGOS — V48.3.16. Cards aproximados da linguagem visual da Galeria. */
 .jogos-shell { width: min(1120px, calc(100% - 32px)); margin: 28px auto 56px; display: grid; gap: 14px; }
 .jogos-intro { padding: 30px; }
 .jogos-intro h1 { margin: 10px 0 7px; color: var(--primary-color); font-size: clamp(2.1rem, 5vw, 3.25rem); font-weight: 900; line-height: 1.05; letter-spacing: -.035em; }
@@ -10,21 +10,25 @@
 .jogos-search { flex: 1 1 250px; min-height: 44px; min-width: 0; padding: 0 14px; color: var(--text-color); background: var(--nav-bg); border: 1px solid var(--card-border); border-radius: 14px; font: inherit; font-size: .9rem; font-weight: 700; }
 .jogos-search:focus { border-color: var(--primary-color); box-shadow: 0 0 0 3px var(--primary-soft); outline: none; }
 .jogos-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 14px; }
-.jogo-card { min-width: 0; overflow: hidden; }
+.jogo-card { position: relative; min-width: 0; overflow: hidden; isolation: isolate; border: 2px solid var(--card-border); border-radius: 20px; box-shadow: var(--shadow-card); transition: transform .2s ease, border-color .2s ease, box-shadow .2s ease; }
+.jogo-card:hover, .jogo-card:focus-within { transform: translateY(-3px); border-color: var(--primary-color); box-shadow: 0 14px 34px rgba(0, 0, 0, .20); }
 .jogo-card-media { position: relative; aspect-ratio: 2 / 3; overflow: hidden; background: var(--primary-soft); }
 .jogo-card-placeholder { position: absolute; inset: 0; display: grid; place-content: center; justify-items: center; gap: 12px; padding: 20px; background: radial-gradient(circle at 50% 42%, var(--primary-soft), transparent 58%), var(--nav-bg); text-align: center; }
-.jogo-card-placeholder::before { content: ""; position: absolute; inset: 12px; border: 1px solid var(--card-border); border-radius: calc(var(--radius-card) - 8px); opacity: .72; }
 .jogo-card-placeholder-mark { width: 58px; height: 58px; display: block; background: var(--primary-color); -webkit-mask: url('https://assets.kamylisumire.com/logo.webp') center / contain no-repeat; mask: url('https://assets.kamylisumire.com/logo.webp') center / contain no-repeat; filter: drop-shadow(0 6px 16px var(--primary-soft)); opacity: .88; }
 .jogo-card-placeholder-text { color: var(--subtitle-color); font-size: .76rem; font-weight: 800; letter-spacing: .01em; }
-.jogo-card-media img { position: relative; z-index: 1; width: 100%; height: 100%; display: block; object-fit: cover; }
-.jogo-card-content { display: grid; gap: 7px; padding: 14px; }
-.jogo-card-content h2 { margin: 0; overflow: hidden; color: var(--text-color); font-size: .98rem; font-weight: 900; line-height: 1.25; text-overflow: ellipsis; white-space: nowrap; }
-.jogo-card-status { width: fit-content; max-width: 100%; padding: 5px 9px; overflow: hidden; color: var(--primary-text); background: var(--primary-soft); border-radius: 999px; font-size: .76rem; font-weight: 800; text-overflow: ellipsis; white-space: nowrap; }
+.jogo-card-media img { position: relative; z-index: 1; width: 100%; height: 100%; display: block; object-fit: cover; transition: transform .34s ease; }
+.jogo-card:hover .jogo-card-media img { transform: scale(1.018); }
+.jogo-card-shade { position: absolute; inset: 34% 0 0; z-index: 2; background: linear-gradient(to bottom, transparent 0%, rgba(12, 7, 14, .13) 24%, rgba(12, 7, 14, .64) 68%, rgba(12, 7, 14, .92) 100%); pointer-events: none; }
+.jogo-card-content { position: absolute; inset: auto 0 0; z-index: 3; display: grid; gap: 5px; padding: 44px 14px 13px; color: #fff; text-shadow: 0 2px 10px rgba(0, 0, 0, .82); pointer-events: none; }
+.jogo-card-content h2 { margin: 0; overflow: hidden; color: #fff; font-size: clamp(.96rem, 1.7vw, 1.12rem); font-weight: 900; line-height: 1.18; letter-spacing: -.012em; text-overflow: ellipsis; white-space: nowrap; }
+.jogo-card-status { overflow: hidden; color: rgba(255, 255, 255, .9); font-size: .78rem; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }
+.jogo-card-steam { width: fit-content; margin-top: 5px; color: #fff; font-size: .76rem; font-weight: 900; text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 3px; pointer-events: auto; }
+.jogo-card-steam:hover, .jogo-card-steam:focus-visible { color: #fff; text-decoration-thickness: 2px; outline: none; }
 .jogos-state, .jogos-notice { padding: 18px 20px; }
 .jogos-state { margin: 0; color: var(--subtitle-color); font-weight: 700; text-align: center; }
 .jogos-state[hidden] { display: none; }
 .jogos-notice { margin-top: 4px; }
 .jogos-notice h2 { margin: 0 0 7px; color: var(--text-color); font-size: 1rem; font-weight: 900; }
 .jogos-notice p { margin: 0; color: var(--subtitle-color); font-size: .84rem; font-weight: 600; line-height: 1.6; }
-@media (max-width: 760px) { .jogos-shell { width: min(100% - 24px, 1120px); margin-top: 20px; margin-bottom: 40px; } .jogos-intro { padding: 24px; } .jogos-intro h1 { font-size: 2.15rem; } .jogos-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; } .jogo-card-content { padding: 11px; } }
+@media (max-width: 760px) { .jogos-shell { width: min(100% - 24px, 1120px); margin-top: 20px; margin-bottom: 40px; } .jogos-intro { padding: 24px; } .jogos-intro h1 { font-size: 2.15rem; } .jogos-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }  }
 @media (max-width: 380px) { .jogos-grid { grid-template-columns: 1fr; } }
diff --git a/jogos/index.html b/jogos/index.html
index 8a1e8b3..5ff8d76 100644
--- a/jogos/index.html
+++ b/jogos/index.html
@@ -246,7 +246,7 @@
     <link rel="stylesheet" href="../css/core/variables.css?v=48.3.7">
     <link rel="stylesheet" href="../css/core/global.css?v=48.3.7">
     <link rel="stylesheet" href="../css/core/navbar.css?v=48.3.7">
-    <link rel="stylesheet" href="../css/pages/jogos.css?v=48.3.15">
+    <link rel="stylesheet" href="../css/pages/jogos.css?v=48.3.16">
 
     <!-- Dados estruturados para mecanismos de busca -->
     <script type="application/ld+json">
@@ -320,7 +320,7 @@
     <script src="../js/core/page-transitions.js?v=46.3"></script>
     <script src="../js/core/sanitize.js?v=1"></script>
     <script src="../js/core/footer.js?v=47.4.2"></script>
-    <script src="../js/pages/jogos/jogos.js?v=48.3.15"></script>
+    <script src="../js/pages/jogos/jogos.js?v=48.3.16"></script>
     <script src="../js/core/loader.js?v=48.3.6"></script>
 </body>
 </html>
diff --git a/js/pages/jogos/jogos.js b/js/pages/jogos/jogos.js
index eb64be5..681026b 100644
--- a/js/pages/jogos/jogos.js
+++ b/js/pages/jogos/jogos.js
@@ -65,6 +65,10 @@
             media.append(image);
         }
 
+        const shade = document.createElement("div");
+        shade.className = "jogo-card-shade";
+        shade.setAttribute("aria-hidden", "true");
+
         const content = document.createElement("div");
         content.className = "jogo-card-content";
         const title = document.createElement("h2");
@@ -73,7 +77,17 @@
         list.className = "jogo-card-status";
         list.textContent = game.listName;
         content.append(title, list);
-        article.append(media, content);
+        if (game.steamUrl) {
+            const steamLink = document.createElement("a");
+            steamLink.className = "jogo-card-steam";
+            steamLink.href = game.steamUrl;
+            steamLink.target = "_blank";
+            steamLink.rel = "noopener noreferrer";
+            steamLink.textContent = "Ver na Steam";
+            steamLink.setAttribute("aria-label", `Ver ${game.name} na Steam`);
+            content.append(steamLink);
+        }
+        article.append(media, shade, content);
         return article;
     }
 
